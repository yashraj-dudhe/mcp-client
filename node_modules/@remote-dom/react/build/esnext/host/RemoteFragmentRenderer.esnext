import { jsx, Fragment } from 'react/jsx-runtime';
import { renderRemoteNode } from './node.esnext';
import { useRemoteReceived } from './hooks/remote-received.esnext';

const RemoteFragmentRenderer = function RemoteFragmentRenderer2({
  element,
  receiver,
  components
}) {
  const fragments = useRemoteReceived(element, receiver);
  if (!fragments) return null;
  const renderOptions = { receiver, components };
  return /* @__PURE__ */ jsx(Fragment, { children: fragments.children.map(
    (child) => renderRemoteNode(child, renderOptions)
  ) });
};

export { RemoteFragmentRenderer };
